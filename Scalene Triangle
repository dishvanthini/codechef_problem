#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);

    while (t--) {
        int a, b, c;
        scanf("%d %d %d", &a, &b, &c);
        // Your code goes here
        if(a!=b && b!=c){
            printf("YES\n");
            
        }
        else{
            printf("NO\n");
        }
    }
return 0;
    
}
//Scalene Triangle whether the triangle is scalene.
